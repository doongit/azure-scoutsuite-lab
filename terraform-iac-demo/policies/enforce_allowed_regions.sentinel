import "tfplan/v2" as tfplan

allowed_regions = json.unmarshal(file("allowed_regions.json"))

main = rule {
  all tfplan.resource_changes as _, rc {
    not rc.change.after.location or
    rc.change.after.location in allowed_regions
  }
}
